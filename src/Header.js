import React from 'react';

const Header = () => {
    return (
        <header>
            <h1 style={{color:"blue", padding:"0px"}}>Add Task</h1>
        </header>
    );
};

export default Header;